{
  "name": "satanblowme.github.io",
  "version": "1.0.0",
  "description": "Spree store with Neocities deployment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "fetch-data": "dotenv -e .env -- node scripts/fetchSpreeData.js",
    "generate-site": "dotenv -e .env -- node scripts/generateSite.js",
    "sync": "dotenv -e .env -- node scripts/syncProducts.js",
    "sync-square": "dotenv -e .env -- node scripts/syncSpreeToSquare.js",
    "deploy": "dotenv -e .env -- npm run fetch-data && dotenv -e .env -- npm run generate-site && dotenv -e .env -- node scripts/deployNeocities.js",
    "dev": "dotenv -e .env -- npm run fetch-data && dotenv -e .env -- npm run generate-site"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/satanblowme/satanblowme.github.io.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/satanblowme/satanblowme.github.io#readme"
  },
  "homepage": "https://github.com/satanblowme/satanblowme.github.io#readme",
  "dependencies": {
    "axios": "^1.6.0",
    "ejs": "^3.1.9",
    "neocities": "^0.0.3"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "webpack": "^5.96.1",
    "webpack-cli": "^5.1.4"
  }
}
